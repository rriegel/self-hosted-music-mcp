# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Secrets — never commit tokens
.env
*.token

# OS
.DS_Store

# spike scripts only write files when --out is passed
spike_*.json

# local index caches (default location would otherwise be committable)
library-index.db
*.db
*.db-wal
*.db-shm
